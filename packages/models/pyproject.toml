[project]
name = "db-mcp-models"
version = "0.3.0"
description = "Shared models for db-mcp"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
